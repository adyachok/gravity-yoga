export class TotalModel {
  totalPrice: number;
  totalDiscount: number;

  constructor(totalPrice: number, totalDiscount: number) {
    this.totalPrice = totalPrice;
    this.totalDiscount = totalDiscount;
  }
}
